<%= semantic_form_for [:admin, entry], as: :entry, url: admin_text_management_update_path do |f| %>
  <%= f.inputs name: "Text entry for #{entry.identifier}" do %>
    <%= f.input :key, as: :hidden %>
    <%= f.input :key, label: 'System key', input_html: {disabled: true} %>
    <%= f.input :identifier, label: 'Identifier', input_html: {disabled: true} %>
    <%= f.input :en_value, as: :text, label: 'English text', input_html: {rows: 4} %>
  <% end %>

  <%= f.actions do %>
    <%= f.action :submit, label: 'Save' %>
    <%= f.action :cancel, label: 'Cancel', wrapper_html: {class: 'cancel'}, url: admin_text_management_path %>
  <% end %>
<% end %>

apiVersion: v1
kind: Service
metadata:
  namespace: climate-watch
  labels:
    name: india-platform
  name: india-platform
spec:
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 3000
  selector:
    name: india-platform

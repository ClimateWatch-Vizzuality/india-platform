apiVersion: v1
kind: Service
metadata:
  namespace: climate-watch
  labels:
    name: india-platform-staging
  name: india-platform-staging
spec:
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 3000
  selector:
    name: india-platform-staging
